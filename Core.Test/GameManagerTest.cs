using System;
using NUnit.Framework;
using Core.Action;

namespace Core.Test
{
    [TestFixture]
    public class GameManagerTest
    {
        [Test]
        public void TestPlayerThrowCubesOrder()
        {
            /*GameManager gameManager = new GameManager();

            Player player1 = new Player();
            Player player2 = new Player();
            Player player3 = new Player();

            gameManager.AddPlayer(player1);
            gameManager.AddPlayer(player2);
            gameManager.AddPlayer(player3);

            assertPlayerHasAction(player1, typeof(ThrowCubesAction));
            assertPlayerHasNotAction(player2, typeof(ThrowCubesAction));
            assertPlayerHasNotAction(player3, typeof(ThrowCubesAction));*/
        }
    }
}
